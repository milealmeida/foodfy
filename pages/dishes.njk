{% extends 'layout.njk' %}

{% block head %}
        <title>Receitas - Foodfy</title>
{% endblock %}

{% block content %}
    <div class="dishes">

        {% for item in items %}
        <div class="dish" id="{{item.id}}">
            <img src="{{item.image}}" alt="Imagem {{item.title}}">
            <h1>{{item.title}}</h1>
            <p>{{item.author}}</p>
        </div>
        {% endfor %}

        {% for item in items %}
        <div class="dish" id="{{item.id}}">
            <img src="{{item.image}}" alt="Imagem {{item.title}}">
            <h1>{{item.title}}</h1>
            <p>{{item.author}}</p>
        </div>
        {% endfor %}

    </div>

<script src="/scripts/script.js"></script>

{% endblock %}